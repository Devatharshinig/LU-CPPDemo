#include <iostream>

using namespace std;

int main()
{
    int a,b;
    int printAnswer(int a,int b);
    cout<<"Enter two numbers to add : ";
    cin>>a>>b;
    cout<<"Sum of "<<a<<" and "<<b<<" is : "<<printAnswer(a,b);
    return 0;
}
int printAnswer(int a,int b){
    return a+b;
}
